# Agent Hub — environment template
#
# No environment variables are needed yet (specification phase).
# When one appears, add it here with a placeholder value and a one-line comment;
# real values go in .env, which is gitignored.
